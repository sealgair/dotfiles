if [ -a /www/$1 ]
then
cd /www/$1
else
cd $1
fi

source bin/activate
cd src

proj_dir=`ls */manage.py 2> /dev/null | sed s/manage.py//g`
if [ -n "$proj_dir" ]
then
cd $proj_dir
fi

#source /www/django/src/django-1.2.1/extras/django_bash_completion
